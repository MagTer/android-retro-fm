<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- PlayerManager's NetworkCallback (instant reconnect when connectivity returns). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name="com.retrofm.android.playback.RetroFmPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Serves remote album art as content:// URIs, the only artwork scheme AAOS renders.
             exported so the car's media host (a separate process) can read the art; safe
             because AlbumArtContentProvider only fetches an allowlisted set of image hosts. -->
        <provider
            android:name="com.retrofm.android.playback.AlbumArtContentProvider"
            android:authorities="com.magter.retrofm.artwork"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
